#include <stdio.h>
/**
 * main -this betty is annoying
 *
 * Return: she is annoying ohhhhh
 */
int main(void)
{
	char letter;

	for (letter = 'z'; letter >= 'a'; --letter)
		putchar(letter);
	putchar('\n');
	return (0);
}
